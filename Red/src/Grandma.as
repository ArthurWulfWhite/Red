package  
{
	import flash.display.BitmapData;
	/**
	 * ...
	 * @author Arthur Wulf
	 */
	public class Grandma extends Dmut
	{
		
		public function Grandma() 
		{
			super(new BitmapData(200, 200, false, 0xA6396A));
		}
		
		/*
		override protected function drawSelf():void 
		{
			graphics.drawRect( -7, -10, 14, 13);
			graphics.moveTo( - 10, -10);
			graphics.lineTo( 0, -17);
			graphics.lineTo(10, -10);
			graphics.lineTo( - 10, -10);
		}
		*/
		
	}

}